#include <stdio.h>
int main()
{
    int n1;
    int n2;
    scanf("%d",&n1);
    scanf("%d",&n2);
    int d=0;
    if(n1>n2)
    {
        for(int i=1;i<=n2;i++)
        {
            if(n1%i==0 && n2%i==0)
            {
                d++;
            }
        }
    }
    else if(n2>n1)
    {
        for(int i=1;i<=n2;i++)
        {
            if(n1%i==0 && n2%i==0)
            {
                d++;
            }
        }
    }
    if(d==1)
    {
        printf("Realtively prime");
    }
    else
    {
        printf("Not");
    }
}
